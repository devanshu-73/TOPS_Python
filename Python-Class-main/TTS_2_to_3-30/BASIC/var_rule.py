'''

    Keyword : A word that conatin a specific meaning in a programming
        there are mainly 33 keywords in python

    Variable : Container to store value

    Syntax : var_name = value

    In Python we never need to declare variable

    Rule to define a variable

        - var name only contains alphabets, numbers,underscore(_)
        - can never starts with numbers
            eg : _name12 <- right ,
                90_name <- wrong
        - var name never contain space between
            eg : my var <- wrong
                my_var <- right
        - any keyword can not be consider as var name
        - var name are case sensitive
            eg : name,Name,NAme,NaMe,etc...           
        
'''
